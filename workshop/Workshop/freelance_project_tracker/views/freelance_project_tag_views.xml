<odoo>
    <record id="project_tag_view_form" model="ir.ui.view">
        <field name="name">project.tag.form</field>
        <field name="model">project.tag</field>
        <field name="arch" type="xml">
            <form string="Project Tags">
                <sheet>

                </sheet>
            </form>
        </field>
    </record>

    <record id="project_tag_view_list" model="ir.ui.view">
        <field name="name">project.tag.list</field>
        <field name="model">project.tag</field>
        <field name="arch" type="xml">
            <list string="Project Tags">
               
            </list>
        </field>
    </record>

    <record id="project_tag_action" model="ir.actions.act_window">
        <field name="name">Project Tags</field>
        <field name="res_model">project.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>